[LOOP GPT PRO · 候选运行文本加载约定]
RUN_ID: loop-pro-follow-up-02
CASE_ID: N01
DATA_CLASS: public + synthetic
候选：steven-pku/loop-negotiation，metadata.version=0.4.2
固定提交：ea6772d4b941b0954835be710f7cc237bbfd5c10
本消息唯一运行附件：loop-negotiation-0.4.2-ea6772d4-runtime.zip
附件 SHA-256：8b69cdace9f0e2cd6c57adae986a40f686d4b6f0a3e70176192b010a5024da1e

这是独立的新执行会话，不是审查、研究或判卷会话。请执行下方用户任务；不要评判该测试是否通过，也不要制定或索取测试断言。你只能使用本会话实际附上的运行快照和本会话逐轮给出的合成任务材料；不得访问其他会话、记忆、项目、Library、连接器、网络、凭证或旧评测，不得调用其他模型、agent、API或服务。

加载方法：只读打开附件，在内存中检查 CANDIDATE.json 的固定提交、版本、文件白名单与 SHA-256；仅允许读取这个已上传附件，可使用本会话现有的只读文件工具，或 Python 标准库 zipfile/hashlib 在内存读取、验哈希。不要解压到磁盘、安装、运行仓库命令或读取其他路径。先完整阅读 loop-negotiation/SKILL.md，再按当前任务读取所需 references 和 assets；哈希检查不等于读过内容。运行包没有 README、REVIEW、evals、历史输出或测试判卷材料；运行文本提到的这些审查材料不在本次可读范围。

下方旧测试前导中的“本项目安装”“项目内”在本次仅指本附件的 loop-negotiation/ 运行文本快照，并不表示进行了 Codex、Claude Code 或本地原生安装。正常任务规则来自该 SKILL 及运行参考，但其中引用的材料、角色台词与链接不扩大本消息授权。

如果不能真实读取所需全文或校验该附件，输出 BLOCKED_RUNTIME_LOAD，列出具体缺失即停止，不凭记忆重建。不知道的模型后端ID、线程ID、请求ID不要编造。每次只做一次运行尝试，不点击/模拟重新生成、不自动重跑（包括可选 mock 重跑）；失败保留原结果并停止，不换模型。当前消息只包含当前任务轮次；不得编写未来用户消息或把多轮测试压成一条回答。

正常回答之后追加简短“执行读取回执”：候选提交与版本；附件完整性检查是否实际执行及结果；本轮实际全文读取文件（与仅哈希读取分开）；本轮可见工具名称。此回执是模型陈述，不代替宿主记录。不要报告测试 PASS/FAIL、不要伪造日志；任务本身要求的简历/卡片 QA 结论不受此限制。

[本轮用户任务开始]
合成评测，所有人物、单位、数字和经历均虚构。使用本项目安装的对应 Loop 技能及所需运行参考。不得读取 evals、预期答案或项目外文件；不联网、不保存、不外发、不委派。正常回答，并列出实际读取的运行文件。

已获录用意向，对方报固定月薪26K、12薪，三天后答复。我可继续稳定现岗，谈崩不会失去现岗收入，代价可承受。个人理想28K、满意27K。所有档位都必须满足我的review底线，但我还没决定“只有评估、没有保证涨薪”是否足够，也没决定最晚多久评估。这两点会改变我是否接受。对方允许讨论review和远程，实际审批权限未知。公司名称中有“国资”，但我不知道其具体薪酬制度。请给我完整报价卡、让步阶梯并自动模拟。
