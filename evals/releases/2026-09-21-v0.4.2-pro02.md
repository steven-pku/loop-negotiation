# v0.4.2 定向验收记录

日期：2026-09-21（Asia/Singapore）。审查标识：`loop-pro-final-review-04`；执行任务标识：`loop-pro-follow-up-02`。

## 结论

**READY — 限定范围的固定运行候选验收。** 本仓4例PASS、共5条回传用户消息。所有结果均为`ACCEPTED_LIMITED`。没有需要追加的运行修复或重跑；应用本文档补丁不改变运行文件。实际发布不是本记录的授权动作。

运行候选：`ea6772d4b941b0954835be710f7cc237bbfd5c10`；版本：`0.4.2`；修复基线：`f41a77f3b3fc2b4acfc66a3773eae0570d7fa178`。实际执行使用runtime ZIP附件，不是原生安装。最终包含本验收文档的提交尚未在本审查环境创建／上传；本记录不预造其SHA。

## 逐例与材料

详见 [4例判卷](2026-09-21-v0.4.2-pro02/GRADING.md)、[manifest](2026-09-21-v0.4.2-pro02/manifest.json)、[发现追加](2026-09-21-v0.4.2-pro02/FINDINGS-ADDENDUM.md)。原始输入输出和操作者记录在 `cases/`；输出行号取自保存的UTF-8文本，不是原生平台导出行号。

N01–N04 四例定向检查通过，共5条真实用户消息。N01为同一会话两轮停止／恢复；N02为一次实际调用中的一靶三组自动对答，验证披露时序及2/2修改上限；N03/N04为局部QA。

## 方法与证据强度

UI照录标签为6 Pro、能力Pro、临时／不个性化；每例新会话、同例逐轮执行。主控只操作和转存，当前GPT Pro按原标准判卷，没有调用新模型、其他agent或自动重跑。原生后端模型和线程ID未暴露，读取/校验/工具回执仅为模型陈述。没有连续录屏、截图或官方导出；协议v2允许逐轮可见文字、工具标签及前轮历史片段合并使用。标签句柄本身不构成后台身份。

新回传ZIP的SHA-256为`a1922fd85fb9d52b05dbd499f48913c83803ade5c88eb6d5a11c30fb819f2b9f`。本次重算53个索引文件哈希、9条原计划输入、4处跨轮前文片段均匹配。这些校验确认收到的文件一致性，不认证执行来源。全批计数不扩大为本仓全功能通过率。

## 未执行与未认证

| 项目 | 状态 |
|---|---|
| 本版原生远端安装 | NOT_RUN |
| 本版Codex／Claude Code原生首次使用 | NOT_RUN |
| 本版CI工作流执行 | NOT_RUN |
| 完整功能回归、跨模型／跨宿主、真实业务结果 | NOT_RUN |
| 最终文档提交上传、标签回读、正式Release创建 | NOT_RUN |
| 原生线程／后端身份 | NOT_EXPOSED / NOT_INDEPENDENTLY_VERIFIED |
| 全部工具／网络／文件读取审计 | NOT_CAPTURED / MODEL_REPORTED_ONLY |
| 发布授权 | NOT_GRANTED |

上述未执行项不冒充通过；READY只覆盖本批已经约定并实际返回的定向检查，不保证通用兼容性、宿主安全或业务结果。上传和发布需要用户另行授权，不新增研究或判卷职责给主控。

## 历史保留

v0.4.1保留首轮10 PASS、2 PARTIAL、4个无输出服务失败及随后8例定向复测。旧首轮与最终运行版不同；本次四例不等于旧十六例在0.4.2上的全量重跑。

旧记录见[上一版本验收](2026-09-20-v0.4.1.md)。不修改旧manifest、输入输出、失败或旧标签；本记录为独立追加。新文档仅改README、英文README、REVIEW、CHANGELOG及新增本日期目录；SKILL、references、assets、SECURITY、LICENSE和工作流均保持候选字节。
