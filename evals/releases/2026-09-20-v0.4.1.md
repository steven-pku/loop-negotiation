# v0.4.1 acceptance record

2026-09-20. Final runtime commit: `6591b8522684d2592d2ddb05747a53f94d017f4f`.

First round: 16 requests, 10 PASS, 2 PARTIAL and 4 service usage-limit failures without outputs. One bounded follow-up on the repaired runtime: 8/8 PASS (the four service cases retried once, two partial cases, and two new forward controls). A clean archive completed three actual continuous turns with correct cumulative limits. The final runtime was not retested against every first-round case.

The [manifest and synthetic input/output artifacts](2026-09-20-v0.4.1/manifest.json) preserve round-specific identities and original verdicts. All people, organizations and career or account claims in these fixtures are fictional. Old failures are not converted into passes.

## Method and limits

Separate Codex agents graded actual outputs; the maintainer inspected critical evidence. This is not a new external cross-model review. Configured model: `gpt-6-astra`, reasoning `medium`, CLI `0.155.1`; no provider/model substitution. Backend identity was not independently authenticated.

- First-round runtime 6077d16 differs from final 6591b85; the final eight-case targeted pass is not a full sixteen-case final-runtime pass. Historical partials and service failures remain visible.
- Continuous conversation only covers single-sentence editing from seeded 1/2 to actual 2/2, bare Halt and explicitly extended 3/3; it does not constitute a full negotiation mock.
- All people, organizations and numerical examples are synthetic. No real negotiation, salary benchmark, legal review or outcome guarantee was tested.
- Mock role-play shares model context; Persona State is a behavioral constraint, not hard information isolation or an independent adversary.
- Read-only host constraints contributed to containment. Host startup and background connection diagnostics may occur; agent-only event inspection does not establish a network-silent host or Skill-only safety.
- Nine-skill routing used the preceding 6077d16 snapshot; metadata remained unchanged. It is not a full final-runtime routing regression.
- Other hosts/models and real-world performance remain unverified; configured model identity is not independent backend authentication. Raw host traces remain private.
